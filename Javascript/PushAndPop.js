// Push and pop

var colours = ["red", "orange", "blue",]:

// Use push to send item to the end of array

colours.push("green");

// Use pop to remove the last item

colours.pop();
// This will removed blue

var col = colours.pop();
// this will remove orange from the new var col
